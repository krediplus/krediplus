import { Body, Controller, Get, Module, Param, Post } from '@nestjs/common'; import { CurrentUser } from '../common/auth'; import { PrismaService } from '../common/prisma.service';
@Controller() class CreditController {constructor(private db:PrismaService){} @Get('credit-lines') lines(@CurrentUser()u:any){return this.db.creditLine.findMany({where:{userId:u.sub,code:'PRINCIPAL'},orderBy:{id:'asc'}})} @Get('credit-lines/:id') line(@CurrentUser()u:any,@Param('id')id:string){return this.db.creditLine.findFirst({where:{id:Number(id),userId:u.sub,code:'PRINCIPAL'},include:{transactions:{orderBy:{createdAt:'desc'},take:100}}})} @Post('credit-requests') request(@CurrentUser()u:any,@Body()b:any){return this.db.creditRequest.create({data:{userId:u.sub,amountUsd:b.amountUsd,installments:b.installments,purpose:b.purpose||null,lenderStoreId:b.lenderStoreId||null,receiverStoreId:b.receiverStoreId||null,bankAccountId:b.bankAccountId||null}})} }
@Module({controllers:[CreditController]}) export class CreditModule {}
